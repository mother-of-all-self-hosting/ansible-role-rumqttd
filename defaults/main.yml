# SPDX-FileCopyrightText: 2023 Julian-Samuel Gebühr
# SPDX-FileCopyrightText: 2023, 2024 Nikita Chernyi
# SPDX-FileCopyrightText: 2023-2026 Slavi Pantaleev
# SPDX-FileCopyrightText: 2024 Sergio Durigan Junior
# SPDX-FileCopyrightText: 2025 MASH project contributors
# SPDX-FileCopyrightText: 2025, 2026 Suguru Hirahara
#
# SPDX-License-Identifier: AGPL-3.0-or-later

---
# Project source code URL: https://github.com/bytebeamio/rumqtt

rumqttd_enabled: true

rumqttd_identifier: rumqttd
rumqttd_base_path: "/{{ rumqttd_identifier }}"
rumqttd_config_path: "{{ rumqttd_base_path }}/config"

# renovate: datasource=docker depName=bytebeamio/rumqttd versioning=semver
rumqttd_version: 0.19.0

rumqttd_uid: ""
rumqttd_gid: ""

rumqttd_container_image: "{{ rumqttd_container_image_registry_prefix }}bytebeamio/rumqttd:{{ rumqttd_container_image_tag }}"
rumqttd_container_image_tag: "{{ rumqttd_version }}"
rumqttd_container_image_registry_prefix: "{{ rumqttd_container_image_registry_prefix_upstream }}"
rumqttd_container_image_registry_prefix_upstream: "{{ rumqttd_container_image_registry_prefix_upstream_default }}"
rumqttd_container_image_registry_prefix_upstream_default: docker.io/

# The base container network. It will be auto-created by this role if it doesn't exist already.
rumqttd_container_network: "{{ rumqttd_identifier }}"

# Control if the container network is deleted on uninstalling.
rumqttd_container_network_deletion_enabled: true

# Specify how the container publishes its MQTT port (as defined by `rumqttd_container_tcp_port`).
#
# Takes an "<ip>:<port>" or "<port>" value (e.g. "127.0.0.1:2586"), or empty string to not expose.
rumqttd_container_tcp_host_bind_port: "{{ rumqttd_container_tcp_port }}"

# The port number in the container that the MQTT v3.1.1 listener binds to.
rumqttd_container_tcp_port: 1883

# Controls whether an MQTT v5 listener is set up, and how it is exposed.
#
# Clients speaking MQTT v5 cannot use the v3.1.1 listener above (and vice versa),
# so a deployment serving both needs both listeners.
rumqttd_v5_enabled: true
rumqttd_container_v5_port: 1884

# Specify how the container publishes its MQTT v5 port (as defined by `rumqttd_container_v5_port`).
#
# Takes an "<ip>:<port>" or "<port>" value (e.g. "127.0.0.1:2586"), or empty string to not expose.
rumqttd_container_v5_host_bind_port: ""

# Controls whether an MQTT-over-WebSocket listener is set up, and how it is exposed.
rumqttd_websocket_enabled: true
rumqttd_container_websocket_port: 8083

# Specify how the container publishes its WebSocket port (as defined by `rumqttd_container_websocket_port`).
#
# Takes an "<ip>:<port>" or "<port>" value (e.g. "127.0.0.1:2586"), or empty string to not expose.
rumqttd_container_websocket_host_bind_port: ""

# Controls whether rumqttd's HTTP console is set up, and how it is exposed.
#
# The console is an unauthenticated debugging interface which reports the broker's
# configuration, its routing metrics and the state of connected devices, and which
# can change the broker's log level. Publishing it to a host port exposes all of
# that to anyone who can reach the port.
rumqttd_console_enabled: true
rumqttd_container_console_port: 3030

# Specify how the container publishes its console port (as defined by `rumqttd_container_console_port`).
#
# Takes an "<ip>:<port>" or "<port>" value (e.g. "127.0.0.1:2586"), or empty string to not expose.
rumqttd_container_console_host_bind_port: ""

# Controls whether a Prometheus metrics endpoint is set up, and how it is exposed.
#
# rumqttd's own default binds this to the container's loopback interface, where nothing
# can scrape it, which is why it is off here rather than on-but-useless. Turning it on
# binds it on the container network, reachable by a Prometheus running in a container
# listed in `rumqttd_container_additional_networks`.
rumqttd_metrics_prometheus_enabled: false
rumqttd_container_metrics_prometheus_port: 9042

# Specify how the container publishes its Prometheus port (as defined by `rumqttd_container_metrics_prometheus_port`).
#
# Takes an "<ip>:<port>" or "<port>" value (e.g. "127.0.0.1:2586"), or empty string to not expose.
rumqttd_container_metrics_prometheus_host_bind_port: ""

# How often (in seconds) the Prometheus endpoint refreshes the metrics it reports.
rumqttd_metrics_prometheus_interval_seconds: 1

# Credentials that clients must present in order to connect.
#
# When this list is empty, the broker accepts any client that connects — which is
# rumqttd's own default, and is only appropriate on a network you trust.
#
# rumqttd matches these against what the client sends verbatim; it has no notion of
# hashed credentials, so the passwords are stored in plain text in the configuration
# file that this role writes.
#
# Example:
#
# rumqttd_auth_users_custom:
#   - username: someone
#     password: some-password
rumqttd_auth_users: "{{ rumqttd_auth_users_auto + rumqttd_auth_users_custom }}"
rumqttd_auth_users_auto: []
rumqttd_auth_users_custom: []

# Settings applied to every connection on every listener.
rumqttd_connection_timeout_ms: 60000
rumqttd_connection_max_payload_size: 20480
rumqttd_connection_max_inflight_count: 100
rumqttd_connection_dynamic_filters: true

# Settings for the router, which is what moves messages between connections.
rumqttd_router_id: 0
rumqttd_router_max_connections: 10010
rumqttd_router_max_outgoing_packet_count: 200
rumqttd_router_max_segment_size: 104857600
rumqttd_router_max_segment_count: 10

# Additional TOML appended verbatim to the end of the configuration file.
#
# Because TOML keys belong to whichever table was declared last, anything put here
# must open with its own `[section]` header.
rumqttd_configuration_extension_toml: ""

# A list of additional container networks that the container would be connected to.
# The role does not create these networks, so make sure they already exist.
# Use this to expose this container to another reverse proxy, which runs in a different container network.
rumqttd_container_additional_networks: "{{ rumqttd_container_additional_networks_auto + rumqttd_container_additional_networks_custom }}"
rumqttd_container_additional_networks_auto: []
rumqttd_container_additional_networks_custom: []

# A list of extra arguments to pass to the container (`docker create` command)
rumqttd_container_extra_arguments: "{{ rumqttd_container_extra_arguments_auto + rumqttd_container_extra_arguments_custom }}"
rumqttd_container_extra_arguments_auto: []
rumqttd_container_extra_arguments_custom: []

# List of systemd services that the rumqttd systemd service depends on
rumqttd_systemd_required_services_list: "{{ rumqttd_systemd_required_services_list_default + rumqttd_systemd_required_services_list_auto + rumqttd_systemd_required_services_list_custom }}"
rumqttd_systemd_required_services_list_default: "{{ [devture_systemd_docker_base_docker_service_name] if devture_systemd_docker_base_docker_service_name else [] }}"
rumqttd_systemd_required_services_list_auto: []
rumqttd_systemd_required_services_list_custom: []

# List of systemd services that the rumqttd systemd service wants
rumqttd_systemd_wanted_services_list: "{{ rumqttd_systemd_wanted_services_list_default + rumqttd_systemd_wanted_services_list_auto + rumqttd_systemd_wanted_services_list_custom }}"
rumqttd_systemd_wanted_services_list_default: []
rumqttd_systemd_wanted_services_list_auto: []
rumqttd_systemd_wanted_services_list_custom: []

# Controls how long to wait for the container to stop gracefully before killing it.
rumqttd_container_stop_grace_time_seconds: "{{ devture_systemd_docker_base_container_stop_grace_time_seconds }}"

# Controls how long the pre-start cleanup should wait for stale container-name release
# before failing startup explicitly.
rumqttd_container_cleanup_timeout_seconds: "{{ devture_systemd_docker_base_container_cleanup_timeout_seconds }}"

# Controls the delay between pre-start cleanup retries.
rumqttd_container_cleanup_retry_delay_seconds: "{{ devture_systemd_docker_base_container_cleanup_retry_delay_seconds }}"

# Shell command used in the systemd pre-start phase to deterministically remove a stale
# container and wait until Docker releases its name.
rumqttd_container_cleanup_command: |
  i=0;
  while [ "$i" -lt {{ rumqttd_container_cleanup_timeout_seconds }} ]; do
  {{ devture_systemd_docker_base_host_command_docker }} stop -t {{ rumqttd_container_stop_grace_time_seconds }} {{ rumqttd_identifier }} >/dev/null 2>&1 || true;
  {{ devture_systemd_docker_base_host_command_docker }} rm -f {{ rumqttd_identifier }} >/dev/null 2>&1 || true;
  if {{ devture_systemd_docker_base_host_command_docker }} container inspect {{ rumqttd_identifier }} >/dev/null 2>&1; then
  i=$((i + 1));
  sleep {{ rumqttd_container_cleanup_retry_delay_seconds }};
  continue;
  fi;
  if {{ devture_systemd_docker_base_host_command_docker }} info >/dev/null 2>&1; then
  exit 0;
  fi;
  echo "Failed to query Docker daemon while cleaning up {{ rumqttd_identifier }}" >&2;
  exit 1;
  done;
  echo "Failed to remove stale container {{ rumqttd_identifier }} after {{ rumqttd_container_cleanup_timeout_seconds }} attempts" >&2;
  exit 1

# Specify the timezone
rumqttd_environment_variables_tz: UTC

# Additional environment variables.
rumqttd_environment_variables_additional_variables: ""

# rumqttd_restart_necessary controls whether the rumqttd systemd
# service will be restarted (when true) or merely started (when false) by the
# systemd service manager role (when conditional restart is enabled).
#
# This value is automatically computed during installation based on whether
# any configuration files, the systemd service file, or the container image changed.
# The default of `false` means "no restart needed" — appropriate when the role's
# installation tasks haven't run (e.g., due to --tags skipping them).
rumqttd_restart_necessary: false
